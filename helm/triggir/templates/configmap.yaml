apiVersion: v1
kind: ConfigMap
metadata:
  name: "{{ .Release.Name }}-config"
data:
  MIX_ENV: "prod"
  RUN_OUTPUT_ROOT: "{{ .Values.runOutputRoot }}"
  PHX_SERVER: "true"
  # TODO!: allow arbitrary env to be added here!
